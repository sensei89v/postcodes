from setuptools import setup

setup(name='postcodeworker',
      version='0.1',
      description='Package to work with post codes',
      packages=['postcodeworker'],
      package_dir={'postcodeworker':'src'}
     )
